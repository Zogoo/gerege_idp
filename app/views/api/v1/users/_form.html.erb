<%= form_with(model: api_v1_user, url: api_v1_user.persisted? ? api_v1_user_path(api_v1_user) : api_v1_users_path, local: true) do |form| %>
  <% if api_v1_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_v1_user.errors.count, "error") %> prohibited this api_v1_user from being saved:</h2>

      <ul>
        <% api_v1_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
