<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Signing In - GeregeIdP</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= vite_stylesheet_tag "application.css" %>
  <%= vite_javascript_tag "application" %>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center" data-redirect-location="<%= @redirect_location if @redirect_location.present? %>">
  <div class="text-center w-full max-w-md mx-auto px-4">
    <!-- Loading Spinner - Fixed alignment -->
    <div class="flex justify-center mb-6">
      <div class="spinner rounded-full h-16 w-16 border-4 border-gray-200 border-t-blue-600"></div>
    </div>
    
    <!-- Loading Text -->
    <h2 class="text-2xl font-semibold text-gray-800 mb-2">Signing you in...</h2>
    <p class="text-gray-600 mb-6">Please wait while we redirect you to your account.</p>
    
    <!-- Progress Bar -->
    <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
      <div class="h-full bg-blue-600 rounded-full transition-all duration-2000 ease-in-out" style="animation: progress 2s ease-in-out infinite;"></div>
    </div>
  </div>

  <style>
    .spinner {
      animation: spin 1s linear infinite;
    }
    
    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    
    @keyframes progress {
      0% { width: 0%; }
      50% { width: 70%; }
      100% { width: 100%; }
    }
  </style>

  <script>
    // Optimized for faster execution
    document.addEventListener('DOMContentLoaded', function() {
      // Get the redirect location from the server response using data attribute
      const redirectLocation = document.body.dataset.redirectLocation;
      
      if (redirectLocation && redirectLocation.trim() !== '') {
        // Fast redirect with minimal delay
        setTimeout(function() {
          window.location.replace(redirectLocation);
        }, 600);
      } else {
        // Fallback: redirect to root
        setTimeout(function() {
          window.location.replace('/');
        }, 800);
      }
    });
    
    // Fallback in case DOMContentLoaded doesn't fire
    setTimeout(function() {
      const redirectLocation = document.body.dataset.redirectLocation;
      if (redirectLocation && redirectLocation.trim() !== '') {
        window.location.replace(redirectLocation);
      } else {
        window.location.replace('/');
      }
    }, 1500);
  </script>
</body>
</html>
