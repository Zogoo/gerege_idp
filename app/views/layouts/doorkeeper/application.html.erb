<!DOCTYPE html>
<html>
<head>
  <title><%= t('doorkeeper.layouts.application.title') %></title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <%= csrf_meta_tags %>
  <%= vite_client_tag %>
  <%= vite_stylesheet_tag 'application' %>
  <%= vite_javascript_tag 'application' %>
</head>
<body class="bg-gray-50 min-h-screen">
  <nav class="bg-white border-b border-gray-200 shadow-sm">
    <div class="container mx-auto px-4 py-3 flex items-center gap-6">
      <%= link_to t('doorkeeper.layouts.application.title'), oauth_applications_path, class: 'font-bold text-lg text-gray-900 hover:text-indigo-600' %>
      <%= link_to t('doorkeeper.layouts.admin.nav.applications', default: 'Applications'), oauth_applications_path, class: 'text-gray-700 hover:text-indigo-600' %>
      <% if respond_to?(:root_path) %>
        <%= link_to t('doorkeeper.layouts.admin.nav.home', default: 'Home'), root_path, class: 'text-gray-700 hover:text-indigo-600' %>
      <% end %>
    </div>
  </nav>
  <div class="container mx-auto px-4 py-8">
    <% if flash[:notice].present? %>
      <div class="mb-6 p-4 rounded bg-blue-100 text-blue-800">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= yield %>
  </div>
</body>
</html>
